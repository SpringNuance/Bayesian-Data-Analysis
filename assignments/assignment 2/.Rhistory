sequence = seq(0, 1, length=100)
prior_distribution = dbeta(sequence, prior_alpha, prior_beta)
plot(seq(0, 1, length=100), posterior_distribution, ylab='density',
type ='l', col='purple', main='Beta Distribution',legend="prior")
}
library(ggplot2)
theme_set(theme_minimal())
library(tidyr)
library(dplyr)
n <- sum(algae == 1)
y <- length(algae) - a
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha = prior_alpha + y
posterior_beta = prior_beta + n - y
sequence = seq(0, 1, length=100)
prior_distribution = dbeta(sequence, prior_alpha, prior_beta)
plot(seq(0, 1, length=100), prior_distribution, ylab='density',
type ='l', col='purple', main='Prior sensitivity analysis',legend="prior Beta distribution")
}
plotBeta(2,10,n, y)
library(ggplot2)
theme_set(theme_minimal())
library(tidyr)
library(dplyr)
n <- sum(algae == 1)
y <- length(algae) - a
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha = prior_alpha + y
posterior_beta = prior_beta + n - y
sequence = seq(0, 1, length=100)
prior_distribution = dbeta(sequence, prior_alpha, prior_beta)
plot(seq(0, 1, length=100), prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line
}
plotBeta(2,10,n, y)
library(ggplot2)
theme_set(theme_minimal())
library(tidyr)
library(dplyr)
n <- sum(algae == 1)
y <- length(algae) - a
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha = prior_alpha + y
posterior_beta = prior_beta + n - y
sequence = seq(0, 1, length=100)
prior_distribution = dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution = dbeta(sequence, posterior_alpha, posterior_beta)
plot(seq(0, 1, length=100), prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line
}
plotBeta(2,10,n, y)
library(ggplot2)
theme_set(theme_minimal())
library(tidyr)
library(dplyr)
n <- sum(algae == 1)
y <- length(algae) - a
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha = prior_alpha + y
posterior_beta = prior_beta + n - y
sequence = seq(0, 1, length=100)
prior_distribution = dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution = dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution)
}
plotBeta(2,10,n, y)
n <- sum(algae == 1)
y <- length(algae) - a
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution)
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution)
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution,type ='l', col='red')
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution, col='red')
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution)
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
#plot(sequence, prior_distribution, ylab='density',xlab="probability",
type ='l', col='purple', main='Prior sensitivity analysis')
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
#plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis')
line(sequence, posterior_distribution)
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
#plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis')
points(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis')
points(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae) - y
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis')
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis')
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 10))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=100)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10,n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10, n, y)
plotBeta(3,10, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plot_grid(plotBeta(2,10, n, y), plotBeta(4,20, n, y))
library(ggplot2)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plot_grid(plotBeta(2,10, n, y), plotBeta(4,20, n, y))
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plot_grid(plotBeta(2,10, n, y), plotBeta(4,20, n, y))
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(1, 95, legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:2, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:2, cex=0.8)
}
plotBeta(2,10, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x=default,y=default,legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:2, cex=0.8)
}
plotBeta(2,10, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="default",y="default",legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:2, cex=0.8)
}
plotBeta(2,10, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topleft",y="topleft",legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:2, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topleft",y="topleft",legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topright",y="topright",legend=c("Prior dist.", "Posterior dist."),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
library(stringr)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topright",y="topright",legend=c(str_glue("Prior dist. alpha={prior_alpha}{prior_beta}"), str_glue("Posterior dist.")),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
library(stringr)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topright",y="topright",legend=c(str_glue("Prior dist. alpha={prior_alpha} beta={prior_beta}"), str_glue("Posterior dist.")),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
library(stringr)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topright",y="topright",legend=c(str_glue("Prior dist. alpha={prior_alpha} beta={prior_beta}"), str_glue("Posterior dist. alpha={posterior_alpha} beta={posterior_beta}")),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
library(stringr)
y <- sum(algae == 1)
n <- length(algae)
plotBeta <- function(prior_alpha, prior_beta, n, y){
posterior_alpha <- prior_alpha + y
posterior_beta <- prior_beta + n - y
sequence <- seq(0, 1, length=10000)
prior_distribution <- dbeta(sequence, prior_alpha, prior_beta)
posterior_distribution <- dbeta(sequence, posterior_alpha, posterior_beta)
plot(sequence, prior_distribution, ylab='density',xlab="probability",type ='l', col='purple', main='Prior sensitivity analysis', ylim = c(0, 20))
lines(sequence, posterior_distribution, col="red")
legend(x="topright",y="topright",legend=c(str_glue("Prior dist. alpha={prior_alpha} beta={prior_beta}"), str_glue("Posterior dist. alpha={posterior_alpha} beta={posterior_beta}")),
col=c("purple", "red"), lty=1:1, cex=0.8)
}
plotBeta(2,10, n, y)
plotBeta(4,20, n, y)
plotBeta(6,30, n, y)
plotBeta(8,40, n, y)
beta_point_est <- function(prior_alpha, prior_beta, data){
count_y = sum(data == 1)
posterior_mean <- (prior_alpha + count_y) / (prior_alpha + prior_beta + length(data))
return(posterior_mean)
}
print("The point estimate for the algae data is: ")
beta_point_est(prior_alpha = 2, prior_beta = 10, data = algae)
print("The point estimate for the algae_test data is: ")
beta_point_est(prior_alpha = 2, prior_beta = 10, data = algae_test)
beta_interval <- function(prior_alpha, prior_beta, data, prob){
count_y <- sum(data == 1)
n <- length(data)
sequence <- seq(0, 1, length=100)
posterior_distribution = dbeta(sequence, prior_alpha + count_y, prior_beta + n - count_y)
sample <- rbeta(10000000, prior_alpha + + count_y, prior_beta + n - count_y)
upper <- 1 - (1 - prob)/2
lower <- (1 - prob)/2
range <- quantile(sample, probs=c(lower, upper))
#plot(seq(0, 1, length=100), posterior_distribution, ylab='density',
#type ='l', col='purple', main='Beta Distribution')
return(range)
}
print("The posterior intervals for the algae data is: ")
beta_interval(prior_alpha = 2, prior_beta = 10, data = algae, prob = 0.9)
print("The posterior intervals for the algae_test data is: ")
beta_interval(prior_alpha = 2, prior_beta = 10, data = algae_test, prob = 0.9)
# mark_my_assignment()
